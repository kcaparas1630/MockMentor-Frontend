import { FC } from "react";
import VideoTestCard from "./VideoTestCard";

const InterviewIndex: FC = () => {
    // TODO: add conditional statement to check if session is active. if not, redirect to video test card.
    return (
        <VideoTestCard />
    )
}

export default InterviewIndex;
